[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "huali-garbage-core"
version = "0.1.0"
description = "Rust core utilities for HuaLi garbage system"
requires-python = ">=3.10"

[tool.maturin]
module-name = "huali_garbage_core"
features = ["pyo3"]
